version: 2

models:
  - name: mart_customer_portfolio
    description: "Portfolio of customer. level `customer_id`."
    columns:
      - name: customer_id
        data_tests:
          - not_null
          - unique
      #     - relationships:
      #         field: customer_id
      #         to: ref('stg_customers')
      # - name: account_id
      #   data_tests:
      #     - not_null
      #     - relationships:
      #         field: account_id
      #         to: ref('int_accounts_summary')