version: 2

models:
  - name: mart_customer_portfolio
    description: "Portfolio of customer. level `customer_id`."
    columns:
      - name: customer_id
        data_tests:
          - not_null
          - unique
          - relationships:
              arguments:
                field: customer_id
                to: ref('stg_customers')

  - name: mart_ecom_sales_summary

  - name: mart_kissaten__sales__summary_day_hour